package org.jbioframework.library.utilities;

public class Logging {
    //final Logger logger = LoggerFactory.getLogger(Example.class);
    //This piece of code must be placed into each class that needs logging capabilities
}
